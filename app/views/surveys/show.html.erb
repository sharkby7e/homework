<h1><%= @survey.question %></h1>

<%= button_to "Yes", survey_responses_path, params: {survey_id: @survey.id, answer: 'yes'} %>
<%= button_to "No", survey_responses_path, params: {survey_id: @survey.id, answer: 'no'} %>

<br> <br>

<%= link_to "All surveys", surveys_path %>
